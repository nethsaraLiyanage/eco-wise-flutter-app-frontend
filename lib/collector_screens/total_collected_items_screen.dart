import 'package:flutter/material.dart';

class TotalCollectedItems extends StatelessWidget {
  const TotalCollectedItems({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
